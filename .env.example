# Client ID for theme and config (e.g. acme, globex, or default)
CLIENT_ID=default

# SQL Server SA password (change this in production!)
SA_PASSWORD=Umbraco_Docker_P@ss1

# Database name (use unique names per client)
UMBRACO_DB=UmbracoDb

# Umbraco port mapping
UMBRACO_HTTP_PORT=8080
UMBRACO_HTTPS_PORT=8443

# SQL Server port mapping
SQL_PORT=1433

# ASP.NET environment
ASPNETCORE_ENVIRONMENT=Development
